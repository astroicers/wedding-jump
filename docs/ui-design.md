# UI 設計文件

## 設計概述

Wedding Jump 的 UI 設計以婚禮場景為核心，採用暖色系（橘色 Primary）搭配深色背景，營造溫馨又現代的氛圍。介面分為兩大端：

- **玩家端**（手機）— 簡潔、觸控友善、快速操作
- **主持人端**（大螢幕/投影）— 16:9 比例、視覺豐富、觀賞性強

---

## 設計系統摘要

完整設計規範見 [design/style-guide.md](../design/style-guide.md)

### 色彩

| 角色 | 色碼 | 用途 |
|------|------|------|
| Primary | `#f48c25` | 主色調、按鈕、強調、計時器環 |
| Accent Pink | `#ff4d94` | CTA 按鈕、重要操作 |
| Bg Light | `#f8f7f5` | 淺色模式背景 |
| Bg Dark | `#221910` | 深色模式背景（主持人端預設） |
| Zone A | `#ff4d4d` (Red) | 答案區域 A |
| Zone B | `#4d79ff` (Blue) | 答案區域 B |
| Zone C | `#ffcc00` (Yellow) | 答案區域 C |
| Zone D | `#2ecc71` (Green) | 答案區域 D |

### 字體

- **主字體**: Spline Sans (Google Fonts) — 300~800 weight
- **圖標**: Google Material Symbols Outlined

### 設計語彙

- **圓角**: 預設 1rem，大型容器 2-3rem，頭像/膠囊 9999px
- **玻璃效果**: `backdrop-filter: blur(12px)` + 半透明背景
- **發光陰影**: Primary 色發光 `box-shadow: 0 0 40px rgba(244,140,37,0.4)`
- **Mesh 背景**: 深色底 + radial-gradient 橘色光暈

---

## 頁面設計

### 1. 首頁 (`/`)

**目的**: 讓使用者選擇「主持人」或「玩家」身分進入。

```
┌─────────────────────────┐
│                         │
│     Wedding Jump        │
│     ♥ Logo              │
│                         │
│  ┌───────────────────┐  │
│  │   我是主持人        │  │
│  │   Create a Room    │  │
│  └───────────────────┘  │
│                         │
│  ┌───────────────────┐  │
│  │   我是來賓          │  │
│  │   Join a Game      │  │
│  └───────────────────┘  │
│                         │
└─────────────────────────┘
```

**UI 元素**:
- 居中佈局，logo + 標題
- 兩個大型 CTA 按鈕（Primary / Accent）
- 背景使用 mesh gradient

---

### 2. 玩家加入 (`/join`)

**目的**: 輸入房間代碼、暱稱，選擇頭像。

```
┌─────────────────────────┐
│  ← 返回                 │
│                         │
│  Room Code              │
│  ┌───────────────────┐  │
│  │ WED-2024          │  │
│  └───────────────────┘  │
│                         │
│  Your Name              │
│  ┌───────────────────┐  │
│  │ 小明               │  │
│  └───────────────────┘  │
│                         │
│  Choose Avatar          │
│  ┌──┐ ┌──┐ ┌──┐ ┌──┐   │
│  │🐱│ │🐶│ │🐰│ │🐻│   │
│  └──┘ └──┘ └──┘ └──┘   │
│  ┌──┐ ┌──┐ ┌──┐ ┌──┐   │
│  │😊│ │😎│ │🌙│ │⭐│   │
│  └──┘ └──┘ └──┘ └──┘   │
│                         │
│  ┌───────────────────┐  │
│  │     Join Game      │  │
│  └───────────────────┘  │
└─────────────────────────┘
```

**UI 元素**:
- 房間代碼輸入框
- 暱稱輸入框（1-20 字元）
- AvatarSelector 元件：網格排列，16 個頭像（DiceBear API）
  - 分類：animal (6)、character (6)、abstract (4)
  - 選中態：Primary 色邊框環
- Join 按鈕（禁用直到填完必填欄位）

---

### 3. 玩家遊戲 (`/game`)

**目的**: 顯示遊戲區域，玩家移動到答案區。

#### O/X 模式

```
┌─────────────────────────┐
│  Q3/15  ⏱ 24s   Score:350│
│                         │
│  ┌─────────────────────┐│
│  │                     ││
│  │    O 區域 (綠色)     ││
│  │     [玩家頭像們]      ││
│  │                     ││
│  ├─────────────────────┤│
│  │                     ││
│  │    X 區域 (紅色)     ││
│  │     [玩家頭像們]      ││
│  │                     ││
│  └─────────────────────┘│
│                         │
│  新郎最喜歡吃牛排？       │
└─────────────────────────┘
```

#### A/B/C/D 模式

```
┌─────────────────────────┐
│  Q3/15  ⏱ 24s   Score:350│
│                         │
│  ┌──────────┬──────────┐│
│  │ A (紅)   │ B (藍)   ││
│  │  牛排     │  壽司    ││
│  │ [頭像]   │ [頭像]   ││
│  ├──────────┼──────────┤│
│  │ C (黃)   │ D (綠)   ││
│  │  披薩     │ 義大利麵  ││
│  │ [頭像]   │ [頭像]   ││
│  └──────────┴──────────┘│
│                         │
│  新郎最喜歡的食物是？      │
└─────────────────────────┘
```

**UI 元素**:
- **頂部**: 題目進度 (ProgressBar) + 倒數計時 (Timer) + 目前分數
- **中央**: GameArena — 全螢幕遊戲區域
  - AnswerZone 元件：每個答案區域用對應顏色（Zone A-D）
  - 玩家頭像（Avatar 元件，48-64px）在區域中浮動
  - 玩家透過觸控/拖曳移動位置
- **底部**: 題目文字
- **揭曉答案後**: 正確區域顯示 ✓ 標記，加分動畫

---

### 4. 主持人控制台 (`/host`)

**目的**: 建立房間，查看房間代碼，準備開始遊戲。

```
┌───────────────────────────────────────────┐
│                                           │
│         Wedding Jump                      │
│         Host Dashboard                    │
│                                           │
│  ┌─────────────────────────────────────┐  │
│  │  Quiz Master Name                   │  │
│  │  ┌──────────────────────────────┐   │  │
│  │  │ 主持人暱稱                    │   │  │
│  │  └──────────────────────────────┘   │  │
│  │                                     │  │
│  │  [Create Room]                      │  │
│  └─────────────────────────────────────┘  │
│                                           │
│  建立後顯示：                              │
│  ┌─────────────────────────────────────┐  │
│  │  Room PIN: WED-2024                 │  │
│  │  Players: 0                         │  │
│  │                                     │  │
│  │  [Start Game]                       │  │
│  └─────────────────────────────────────┘  │
│                                           │
└───────────────────────────────────────────┘
```

---

### 5. 主持人遊戲 (`/host/game`)

**目的**: 16:9 投影畫面，控制題目進度，顯示即時資訊。

**原型**: [design/stitch-ui.txt](../design/stitch-ui.txt)

```
┌────────────────────────────────────────────────────────────────┐
│ ♥ The Love Story Trivia    ⏱ [24]    👥 142 Players   PIN:2024│
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  ┌────────────────────────────────────────────┐  ┌──────────┐ │
│  │                                            │  │ Controls │ │
│  │              QuestionCard                  │  │          │ │
│  │         「新郎最喜歡的食物是？」              │  │ [Reveal] │ │
│  │                                            │  │ [Next]   │ │
│  │  ┌──────────┐  ┌──────────┐                │  │ [Leader] │ │
│  │  │ A: 牛排   │  │ B: 壽司  │                │  │ [End]    │ │
│  │  │ (12人)    │  │ (8人)    │                │  │          │ │
│  │  ├──────────┤  ├──────────┤                │  └──────────┘ │
│  │  │ C: 披薩   │  │ D: 義大利 │                │              │
│  │  │ (5人)     │  │ (3人)    │                │              │
│  │  └──────────┘  └──────────┘                │              │
│  │                                            │              │
│  └────────────────────────────────────────────┘              │
│                                                                │
│  Question 5 of 15                                             │
└────────────────────────────────────────────────────────────────┘
```

**UI 元素**:
- **Header**: Logo、題目進度、Timer（大型 SVG 環形倒數）、玩家數（PlayerCounter）、房間 PIN
- **中央**: QuestionCard + AnswerGrid（顯示選項及各選項的玩家數）
- **右側**: GameControls — 根據遊戲階段顯示不同按鈕：
  - `waiting` → 「Start Question」
  - `question`/`answering` → 「Reveal Answer」
  - `reveal` + 有下一題 → 「Next Question」+「Show Leaderboard」
  - `reveal` + 最後一題 → 「End Game」+「Show Leaderboard」
  - `ended` → 「Final Rankings」
- **LeaderboardPopup**: 半透明覆蓋層，顯示即時排行榜

### 設計要點：

- 深色背景 + Mesh gradient，適合投影環境
- 計時器使用 SVG 環形動畫，視覺衝擊力強
- 答案選項使用對應 Zone 顏色，直觀區分
- 玻璃效果 (glass) 用於卡片和彈出層

---

### 6. 最終排行榜 (`/rankings`)

**目的**: 遊戲結束後顯示最終排行。

```
┌─────────────────────────┐
│                         │
│    🏆 Final Rankings     │
│                         │
│  ┌─────────────────────┐│
│  │ 🥇 1. 小明   350 pts ││
│  │   [頭像]              ││
│  ├─────────────────────┤│
│  │ 🥈 2. 小華   280 pts ││
│  ├─────────────────────┤│
│  │ 🥉 3. 小美   250 pts ││
│  ├─────────────────────┤│
│  │    4. 小強   200 pts ││
│  │    5. 小玲   180 pts ││
│  │    ...               ││
│  └─────────────────────┘│
│                         │
│  [Back to Home]         │
└─────────────────────────┘
```

**UI 元素**:
- Leaderboard 元件：前三名特殊樣式（金/銀/銅）
- 每行顯示：排名、頭像、暱稱、分數
- 主持人端大螢幕顯示（適合拍照留念）

---

## 元件清單

### 共用元件 (`components/common/`)

| 元件 | 檔案 | 說明 | Props |
|------|------|------|-------|
| Avatar | `Avatar.tsx` | 玩家頭像 | `src`, `name`, `size`, `showName`, `onClick` |
| Button | `Button.tsx` | 通用按鈕 | `variant`, `size`, `icon`, `loading`, `disabled`, `fullWidth` |
| Timer | `Timer.tsx` | 倒數計時器 | `seconds`, `maxSeconds`, `size` |
| Leaderboard | `Leaderboard.tsx` | 排行榜列表 | `players`, `maxDisplay` |
| ProgressBar | `ProgressBar.tsx` | 進度條 | `current`, `total` |

### 主持人元件 (`components/host/`)

| 元件 | 檔案 | 說明 | Props |
|------|------|------|-------|
| QuestionCard | `QuestionCard.tsx` | 題目卡片 | `question`, `showAnswer` |
| AnswerGrid | `AnswerGrid.tsx` | 答案選項格 | `question`, `playerCounts`, `correctAnswer` |
| GameControls | `GameControls.tsx` | 遊戲控制按鈕 | `phase`, `hasNextQuestion`, `onStart/Reveal/Next/End` |
| PlayerCounter | `PlayerCounter.tsx` | 線上玩家數 | `count` |
| LeaderboardPopup | `LeaderboardPopup.tsx` | 排行榜彈出 | `isOpen`, `onClose`, `leaderboard` |

### 玩家元件 (`components/player/`)

| 元件 | 檔案 | 說明 | Props |
|------|------|------|-------|
| GameArena | `GameArena.tsx` | 遊戲區域容器 | `question`, `players`, `onMove` |
| AnswerZone | `AnswerZone.tsx` | 答案區域 | `label`, `zone`, `isSelected`, `isCorrect`, `isRevealed`, `playerCount`, `onClick` |
| AvatarSelector | `AvatarSelector.tsx` | 頭像選擇器 | `selected`, `onSelect` |

---

## 頭像系統

### 預設頭像

使用 [DiceBear API](https://dicebear.com/) 生成 16 個預設頭像，分為三個類別：

| 類別 | 風格 | 數量 | 範例 |
|------|------|------|------|
| Animal | Adventurer | 6 | Cat, Dog, Bunny, Bear, Panda, Fox |
| Character | Avataaars | 6 | Happy, Cool, Chill, Sunny, Luna, Star |
| Abstract | Bottts | 4 | Beep, Boop, Zap, Pixel |

### 頭像尺寸

| 用途 | 尺寸 |
|------|------|
| 排行榜（前三名） | 96-128px |
| 遊戲區域 | 48-64px |
| 列表項目 | 32-40px |
| 最小 | 24px |

### 頭像 URL

```
https://api.dicebear.com/7.x/{style}/svg?seed={name}
```

支援自訂上傳（base64 data URL）和本地路徑（`/avatars/`）。

---

## 響應式設計

| 斷點 | 寬度 | 用途 | 特點 |
|------|------|------|------|
| Mobile | < 640px | 玩家手機 | 觸控區域大、單欄佈局 |
| Tablet | 640-1024px | 平板 | 雙欄可選 |
| Desktop | > 1024px | 主持人螢幕 | 多欄、資訊豐富 |
| TV/Projector | 16:9 | 投影 | 固定比例、深色主題 |

### 玩家端（Mobile-first）

- 遊戲區域佔滿螢幕
- 答案區域觸控面積足夠大
- 頭像選擇器水平滾動
- 文字大小足夠在手持距離閱讀

### 主持人端（Desktop/TV）

- 16:9 佈局適合投影
- 深色主題避免強光
- 大字體適合遠距離觀看
- 計時器和分數醒目顯示

---

## 動畫效果

| 場景 | 動畫 | 實作 |
|------|------|------|
| 按鈕 hover/press | 微縮放 + 陰影變化 | `transition: all 0.3s ease` |
| 計時器倒數 | SVG stroke-dashoffset 動畫 | `countdown-ring` class |
| 答案揭曉 | 正確區域放大 + 發光 | Tailwind `animate-pulse` |
| 新玩家加入 | 淡入 | `animate-fadeIn` |
| 分數增加 | 數字跳動 | `animate-bounce` |
| 排行榜變動 | 列項滑動 | CSS transition |
| 載入中 | Spinner 旋轉 | `animate-spin` |

---

## 設計資源

| 資源 | 路徑 | 說明 |
|------|------|------|
| 設計規範 | `design/style-guide.md` | 色彩、字體、間距、圓角、陰影 |
| 主持人原型 | `design/stitch-ui.txt` | HTML + Tailwind 原型（16:9） |
| 頭像配置 | `client/src/utils/avatars.ts` | DiceBear API 頭像定義 |
| Zone 配置 | `client/src/utils/constants.ts` | O/X 和 A/B/C/D 區域顏色定義 |
| 頭像資源 | `design/assets/avatars/` | 靜態頭像圖片（如有） |
